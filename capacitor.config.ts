import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'svelte-capacacitor-safearea',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
